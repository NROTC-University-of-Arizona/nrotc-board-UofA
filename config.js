// Public display settings only. Publishing is managed through the GitHub repository.
window.BOARD_CONFIG = Object.freeze({
  demoMode: true // Set false after replacing the example notices with real public notices.
});
